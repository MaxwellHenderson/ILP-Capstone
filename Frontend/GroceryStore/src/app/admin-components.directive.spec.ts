import { AdminComponentsDirective } from './admin-components.directive';

describe('AdminComponentsDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminComponentsDirective();
    expect(directive).toBeTruthy();
  });
});
