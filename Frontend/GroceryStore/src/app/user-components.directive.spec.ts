import { UserComponentsDirective } from './user-components.directive';

describe('UserComponentsDirective', () => {
  it('should create an instance', () => {
    const directive = new UserComponentsDirective();
    expect(directive).toBeTruthy();
  });
});
